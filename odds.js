function findOdd(A) {
  const set = new Set(A);
  const vocab = Array.from(set).map((el, index) => ({ [el]: 0 }));

  return obj;
}

console.log(findOdd([20, 1, -1, 2, -2, 3, 3, 5, 5, 1, 2, 4, 20, 4, -1, -2, 5]));
